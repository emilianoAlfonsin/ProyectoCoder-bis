//----- Sección en proceso -----

// const cartIcon = document.querySelector('#cart-icon')
// const contadorCarrito = carritoJSON.reduce((acc,prod) => acc + prod.cantidad, 0)

// if(carrito.length > 0){
//     cartIcon.insertAdjacentHTML("beforeend", `<span class="position-relative top-0 start translate-middle badge rounded-pill bg-danger">${contadorCarrito}</span>`)
// }